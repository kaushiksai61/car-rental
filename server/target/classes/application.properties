# =========================
# DATABASE (MySQL)
# =========================
spring.datasource.url=jdbc:mysql://localhost:3306/mydb
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================
# JPA / HIBERNATE
# =========================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=update

# =========================
# SERVER
# =========================
server.port=3000

# =========================
# EMAIL (SMTP) - REQUIRED FOR OTP (GMAIL)
# =========================
# IMPORTANT:
# - Gmail App Password is used (NOT normal Gmail password)
# - Spaces removed from app password
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kaushik.duonomic@gmail.com
spring.mail.password=buka ghrr wsld nwvg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Recommended timeouts (prevents hanging / 500 errors)
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# "From" address shown in email
app.mail.from=kaushik.duonomic@gmail.com

# =========================
# OTP POLICY (OPTION B)
# =========================
# OTP expiry time in minutes
app.otp.expiry.minutes=10

# Max wrong OTP attempts allowed
app.otp.max.attempts=5

# OPTIONAL: Resend control window (anti‑spam)
app.otp.resend.window.minutes=15
app.otp.max.resends.per.window=3

# =========================
# OPTIONAL (ENABLE ONLY IF DEBUGGING EMAIL)
# =========================
# logging.level.org.springframework.mail=DEBUG
# logging.level.com.sun.mail=DEBUG

